{
  "name": "nm-calcados",
  "version": "0.2.0",
  "private": true,
  "description": "ERP/POS integrado da NM Calçados.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check src/app.js && node --check src/config/env.js && node --check src/config/database.js && node --check src/database/migrate.js && node --check scripts/check-db.js && node --check src/routes/index.js && node --check src/middlewares/notFound.js && node --check src/middlewares/errorHandler.js",
    "db:check": "node scripts/check-db.js",
    "db:migrate": "node src/database/migrate.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "mysql2": "^3.24.2"
  }
}
